{
  "name": "BizzFlow (WifiBizz crawler + bill generator)",
  "install": "bash scripts/dev/setup.sh",
  "start": "bash scripts/dev/dev-services.sh",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npm run dev",
      "description": "Next.js 16 dev server on http://localhost:3000. Reads .env (NEON_WS_LOCAL_PROXY) so src/lib/prisma.ts bridges the Neon serverless driver to local Postgres via wsproxy."
    }
  ],
  "ports": [
    { "name": "web", "port": 3000 }
  ]
}
