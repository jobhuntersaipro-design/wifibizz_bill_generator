# Agent logins (this project)

Do **not** put passwords in commits beyond what operators already share out-of-band.

| System | Identifier |
| --- | --- |
| BizzFlow | `aiboot1@gmailcom` (literal — no dot in the domain label). `aiboot1@gmail.com` is **wrong** for this account. |
| Unifi dealer (Order Entry) | Staff `TMRS00517`, registered email `nexion.eform@gmail.com` |

Passwords are the team-known values used on production (`bizzflow.top`).
