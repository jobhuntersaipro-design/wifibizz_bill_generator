# TA + Auth Letter landlord signature, witnesses, Section 4

ClickUp `86eyuua3n`. Signature model A.

## What shipped

- Admin tab `landlord signature` — upload / list / delete PNG or JPEG (R2 + Prisma), same shape as umobile image.
- Each generate invents a landlord (Malay Name + NRIC) and two witnesses. No landlord registry.
- The same `partiesSeed` is sent for TA and Auth Letter on one Case List case / one Order Entry session so both PDFs print the same landlord NAME.
- A random pool image is stamped on the landlord signature line of both docs.
- **Empty pool:** generate still succeeds; the landlord signature line is blank. No hard-fail.
- First Schedule Section 4 premises wrap and shrink inside the particulars cell (`232–538` × `503–556`). Golden case `202666996` no longer overflows.
- Case List Bills icon `Letter` → `Auth Letter`. Order Entry card `Authorization Letter` → `Auth Letter`. `TA` unchanged.

## Not in scope

Internet / Utility / TIME / Chat / combine docs. No full template redesign. Tenant signature stays the drawn mark.
