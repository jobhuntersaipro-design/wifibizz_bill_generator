# Completed feature specs (archive)

Specs for features that have already shipped. They're kept for reference only.

**Active specs live one level up in `context/features/`.** Anything sitting at the
top of that folder is what still needs to be worked on. Move a spec in here once
its feature is merged and recorded in `context/current-feature.md` history.

Currently active (top level):
- `order-entry-build-spec.md` — Unifi eSales Order Entry automation (in progress)
- `selector_map.md` — verified DOM selectors backing the order-entry flow
