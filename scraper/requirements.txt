# Runtime deps for the BizzFlow scraper / order-entry Flask service.
# Pinned to the versions verified locally. Chromium itself comes from the
# Playwright base Docker image (must match playwright's version below).
Flask==2.3.3
Flask-Cors==4.0.0
gunicorn==23.0.0
playwright==1.55.0
playwright-stealth==2.0.2
boto3==1.43.56
cryptography==46.0.3
python-dotenv==1.0.0
requests==2.31.0
# Imported at load by login_manager -> gmail_otp_reader (only used if auto-OTP
# is enabled; the dealer flow types OTP manually, but the import must resolve).
google-api-python-client==2.187.0
google-auth==2.45.0
google-auth-oauthlib==1.2.2
google-auth-httplib2==0.2.1
