[pytest]
# Only tests/ holds real tests. Without this, a bare `pytest` also walks
# devtools/ and the service modules looking for test_*.py — and the root
# used to hold manual_dealer_login.py (then named test_dealer_login.py),
# which reads as a test file but drives a REAL dealer login.
testpaths = tests
